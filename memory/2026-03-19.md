# Journal — 2026-03-19

## Bot Hyperliquid — État du jour

- Bot `hl_webhook_server.py` online, MAINNET, LIVE, port 8080
- Equity au dernier check : ~1043 USDC
- Positions ouvertes : ETH SHORT -0.0093 @ ~2122, SOL SHORT -0.22 @ 88.55
- Sizing v5.3 correct : 10 USDC risque (2% de 500$), leverage 2x ISOLATED
- ngrok static URL active : `https://nonchurchgoing-introvertedly-carolynn.ngrok-free.dev`

## Pine Script — Historique des modifications aujourd'hui

### Problème principal résolu (commit 56b992f)
- Les versions précédentes utilisaient `compute29m()` avec des fonctions imbriquées (`hullMa`, `calcAtr`, `calcDmi` à l'intérieur de `compute29m()`)
- TradingView rejetait avec "Syntax error at input '=>' ligne 78" à chaque collage
- **Fix définitif** : suppression de `compute29m()` et `request.security("29", ...)`, calculs directs sur barres du chart 30M, toutes les fonctions au niveau global
- Fichier final : 292 lignes, zéro fonction imbriquée

### Git commits du jour
- `8cebc92` — fix: v5.3 risk-based sizing + get_account_value unified account (spot USDC)
- `7b2e05c` — feat: JP v5.3 → 29M interne via request.security (chart reste en 30M) ← causait syntax error
- `3aabb86` — fix: alert_message sur strategy.entry + startDate + calc_on_every_tick=false
- `5911234` — revert: restaure script v5.3 fonctionnel
- `56b992f` — fix: suppression compute29m/request.security, calculs directs 30M ← VERSION FINALE

## TradingView — État des alertes

- 2 alertes actives via API pricealerts.tradingview.com :
  - ID `4271185226` : SOLUSDT.P, 30m, `{{alert.message}}`, ngrok ✅
  - ID `4271567892` : ETHUSDT.P, 30m, `{{alert.message}}`, ngrok ✅
- pine_version dans les alertes : 36.0 (mais le message `{{alert.message}}` fonctionne)
- Problème persistant matin : TradingView envoyait le texte littéral `{{alert.message}}` (non remplacé) car le script v39 sur le chart n'avait plus de `alert()` calls
- Fix : JP doit coller le nouveau script (292 lignes) sur les 2 charts → alertes fonctionneront

## Accès écran/clavier macOS

- JP a tenté d'accorder permissions Accessibilité + Enregistrement d'écran à Terminal
- Terminal n'était pas dans la liste → guidé pour utiliser le bouton "+"
- Permissions pas encore confirmées comme accordées

## Moltbook

- Aucune notification, aucun DM non lu toute la journée

## Contexte technique

- `get_account_value()` lit le spot USDC balance (unified account, pas perps directement)
- Transfert Spot→Perps impossible : "Action disabled when unified account is active" — normal
- TradingView `create_alert` / `edit_alert` API : retourne `invalid_request` / `no_such_endpoint` → ne pas retenter l'automatisation de cette API
- Playwright headless sur TradingView : `widgetbar-widgetbody` vide (React virtualization) → inutilisable pour les alertes
